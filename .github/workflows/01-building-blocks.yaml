name: 01 - Building Blocks
on: push
jobs:
    echo-gello:
        runs-on: ubuntu-latest
        steps:
            - name: Say hello
              run : echo "hello, world!"
    ecjho-goodbye:
        runs-on: ubuntu-latest
        steps:
            - name: Failed step
              run: |
               echo "I will fail"
               exit 1
            - name : say goodbye
              run: echo "GoodBye"

